# CEF Framework manifest — top-level metadata for the Framework Loader.
#
# Capability modules are DISCOVERED from the modules/ directory; they are never hardcoded
# in the loader (Framework Discovery, Phase 4). Each module carries its own metadata in a
# module.yaml and its detailed specification in a lazily-loaded spec.md.
name: Claude Enterprise Framework
frameworkVersion: 2
modulesDir: modules
