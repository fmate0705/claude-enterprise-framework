/**
 * Public programmatic entry for `@cef/cli`.
 *
 * The command registry, Commander program, and runtime wiring are introduced in later
 * milestones (M3–M4, `ARCHITECTURE.md` §9–§10). M0 exposes only the version helper.
 */
export { readCliVersion } from './version.js';
