# Design System Memory

**Purpose:** Record the concrete design system for the current project — the resolved tokens and component decisions that implementation must follow.

**Description:** Where the design *standard* defines how CEF thinks about design and the branding memory holds raw brand inputs, this file records the *resolved* design system for the active project: the actual token values, component variants, and patterns chosen. It is written during planning and kept current so every screen is built from the same system rather than improvised.

## Schema

- **Color tokens:** _resolved values — filled during planning_
- **Typography scale:** _resolved values — filled during planning_
- **Spacing scale:** _resolved values — filled during planning_
- **Radius / shadow / elevation:** _resolved values — filled during planning_
- **Component variants in use:** _filled during planning_
- **Theming (light/dark):** _filled during planning_

## Status

Template (AS-000). No active project. Populated by the planning workflow.

## TODO

- [ ] Resolve tokens from branding during planning.
