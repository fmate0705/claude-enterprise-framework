# Architecture Memory

**Purpose:** Record the architectural decisions made for the current project so implementation stays consistent across sessions.

**Description:** This file captures the concrete architecture chosen for the active project: stack, structure, data flow, and integration points. It is written during planning and referenced throughout implementation and maintenance, so the shape of the system is never re-litigated mid-build. It complements the architecture *standard* (which defines defaults) by recording the *project-specific* choices.

## Schema

- **Stack:** _framework, language, key libraries — filled during planning_
- **Directory structure:** _filled during planning_
- **Data flow & state management:** _filled during planning_
- **External services / integrations:** _filled during planning_
- **Notable deviations from the architecture standard (with reasons):** _filled as needed_

## Status

Template (AS-000). No active project. Populated by the planning workflow.

## TODO

- [ ] Populate when architecture is decided in planning.
